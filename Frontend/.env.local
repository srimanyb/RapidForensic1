# Frontend environment configuration for Vercel
# Set the backend API URL in Vercel dashboard under Environment Variables

# For local development (not needed on Vercel)
# REACT_APP_API_BASE=http://localhost:5000

# On Vercel, the frontend will automatically use the same domain as the deployed backend
# Make sure your backend is deployed to a Vercel project and the URL is accessible
